# 第五人格网站：数据与视频解析规则

本项目涉及第五人格数据时，必须优先查阅 `data/DATA_SOURCES.md`，并按其中的来源优先级、赛季时效和证据类型处理。不要只依赖模型记忆或搜索摘要。

## 数据使用约定

- 角色基础数据、排位统计、角色胜率/出场率/禁用率、逐日数据和角色认知徽章分数线：优先解析项目 `data/raw/` 中的快照与已生成的 `data/*.js`，需要最新值时再核对登记的在线来源。
- 赛季坐标图（强度/难度）：只作为对应赛季的巅峰七阶参考，必须标注赛季；新赛季不得沿用旧坐标图，除非用户明确要求比较历史赛季。
- 所有统计回答应说明：赛区（默认国服）、模式/段位、赛季、数据日期或快照日期，以及是否为官方发布值或加权计算值。
- 若来源之间冲突，按 `data/DATA_SOURCES.md` 的优先级处理，并保留冲突说明；不要静默覆盖。

## 视频解析约定

后续用户提供或引用视频时，使用已配置的 `video-use` skill：先取得原始视频/可用字幕，优先使用逐词、逐时间戳转录，必要时在关键结论处做画面核验，并在回答中区分“视频作者观点”和“数据库事实”。视频解析产物应放在视频目录的 `edit/` 下，不写入 skill 自身目录。

## 终端约定

全局 `C:\Users\asus\.codex\RTK.md` 要求命令以 `rtk` 前缀运行。若当前环境没有 `rtk` 可执行文件，应明确记录这一限制，并使用等价的只读 PowerShell 命令继续工作，不要假装已经通过 rtk 执行。
